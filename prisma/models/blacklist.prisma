model Blacklist {
  id      String        @id
  type    BlacklistType
  name    String?
  when    DateTime      @default(now())
  reason  String
  ends_at DateTime?

  @@index([type])
}

enum BlacklistType {
  USER
  GUILD
}
